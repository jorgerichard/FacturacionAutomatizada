# Shared contracts

This folder contains the first API contract draft used by the microservices boundary.

## Routes

- GET /api/health -> returns service name
- GET /api/routes -> returns available upstream routes
